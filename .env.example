# Server-only MongoDB connection string used by the app's database layer.
# Copy this file to `.env.local` for local development and replace the value if needed.
# The default value matches the local Docker Compose MongoDB service in this repo.
MONGODB_URI=mongodb://127.0.0.1:27017/daylo
